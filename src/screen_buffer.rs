use crate::bitmap::GMBitmap;

pub struct GMScreenBuffer {
    // TODO
}

impl GMScreenBuffer {
    pub fn new() -> GMScreenBuffer {
        GMScreenBuffer {
            // TODO
        }
    }

    pub fn blit_bitmap(&mut self, bitmap: &GMBitmap, px: u32, py: u32) {
        // TODO
    }
}
