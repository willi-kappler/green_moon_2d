TODO:

- add more methods for 2d vector.
- add custom settings to configuration class.
- add movement effects.
- add examples for sprite.
- check for more keys / other input.
- add test cases for GMText.
- add test cases for GMSprite.
- add test cases for GMInterpolation.


Maybe:

(add test cases for GMFont.)
(add test cases for GMTexture (GMTextureInterface).)
(add some calculation to GMTextureInterface.)
(rename GMObject to GMGFXObject and create a new simple GMObject as a base.)



Done:

# Add py.typed to src/green_moon_2d/ path:
# https://mypy.readthedocs.io/en/stable/installed_packages.html#installed-packages
# test cases for configuration.
# test scene manager messages.
# add scene_update() and scene_draw() method.
# test cases for context.
# implement __getitem__() for GMObjectManager.
# add tests for object properties.
# move to pygame-ce (from pysdl2)
# align text.
# vertical text.
# add log messages.
# add examples for text.
# add text effects.
# add GMTextureInterface in order to test GMFont, GMText, GMSprite, etc.
# implement GMObject::send_message() for basic settings.
# add "fixed" to GMRepetition.
# add text case for object.move.(dt)
# add more messages to send_message in GMTextEffect1.
# object.add_groups(Iterable)
# object.remove_property(name)
# context.clear() use fill(number) instead of fill(str).
# engine: don't call clear_screen(), let user do it if needed.
# object.add_properties(Iterable).
# om.add_properties_group(Iterable).
# add test cases for sending messages to an object.

